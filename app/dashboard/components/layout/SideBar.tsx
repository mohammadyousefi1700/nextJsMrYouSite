"use client";

import { useState } from "react";

function SideBar() {
  const [isOpen, setIsOpen] = useState<boolean>(false);
  return <div className="w-16 bg-gray-700 "></div>;
}

export default SideBar;
